void main()
{
 object oPC = GetLastUsedBy();
 FloatingTextStringOnCreature("One book reads:'What mysterious happenihgs have their birth here?  Only the greates feats of wizardry, for which every element of earth, water and sky is but a tool!'.", oPC);
}
